import formatMessage from 'format-message';
import React from 'react';
import Container from '../../components/container/Container';

const OrdersPage = () => {
  return <Container title={formatMessage('Mis pedidos')}></Container>;
};

export default OrdersPage;
